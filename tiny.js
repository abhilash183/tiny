
/**
 * This method generates the tiny url from the actual url.
 * Tiny url consists of 6 alphanumeric characters ~= 2billion possibilities
 * @param actual_url: url to shorten
 * @return tiny_url: just the url path (domain name should be filled out)
 */
exports.generate_tiny_url = function(actual_url){
  var tiny_url;

  tiny_url = actual_url;

  return tiny_url;
}
